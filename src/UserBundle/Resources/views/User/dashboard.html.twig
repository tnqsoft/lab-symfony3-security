{% extends 'base.html.twig' %}

{% block body %}
<h1>User Dashboard</h1>
{% if is_granted('IS_AUTHENTICATED_FULLY') %}
    <p>
        Hello {{ app.user.username }}!. [<a href="{{ path('user_logout') }}">Logout</a>]
    </p>
{% endif %}
{% endblock %}
